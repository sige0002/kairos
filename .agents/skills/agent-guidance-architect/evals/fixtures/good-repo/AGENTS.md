# Project Instructions
## Repository overview
ROS 2 + Rust robot data collection system.
## Required commands
- After Rust changes: `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets`
- Run the tests related to your change.
## Change policy
- Do not modify unrelated files.
- Verify implementations before assuming behavior.
