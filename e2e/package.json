{
  "name": "kairos-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "UI-driven acceptance suite for capture store v2 (contract §13). Kept out of services/frontend on purpose: the app must not carry a browser driver into its own image or its dependency tree.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit",
    "report": "playwright show-report",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^22.10.2",
    "typescript": "^7.0.2"
  }
}
